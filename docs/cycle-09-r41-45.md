# Cycle 09 ¡ª Rounds 41¨C45

Security hardening sprint: CORS, alerting, signed webhooks, and
audit exportability. Five rounds focused on making the studio's
security story production-grade and observable.

## Round 41 ¡ª CORS hardening
- Strict mode via LAUNCHLENS_CORS_ORIGINS comma-separated allowlist
- Permissive mode (default) for local development
- 10 CORS e2e tests covering disallowed origins, preflight, same-origin
- All API routes protected: research, csrf, admin tokens, admin audit

## Round 42 ¡ª Auth audit alerting system
- Four detection rules: auth_failed burst, csrf_failed burst,
  rate_limited burst, admin token creation (critical)
- Sliding-window detection per IP hash with cooldown
- 50-alert ring buffer
- Webhook delivery via LAUNCHLENS_ALERT_WEBHOOK_URL
- Admin endpoint: GET /api/admin/alerts, DELETE /api/admin/alerts
- Pub/sub pattern in auth-audit via onAuthAuditEvent()
- 13 unit tests, 16 admin e2e tests

## Round 43 ¡ª HMAC webhook signatures
- X-LaunchLens-Signature header (HMAC-SHA256, sha256=<hex>)
- X-LaunchLens-Timestamp for replay protection
- Signature covers 	imestamp.body
- sent_at field in webhook payload
- LAUNCHLENS_ALERT_WEBHOOK_SECRET env var
- 3 new unit tests

## Round 44 ¡ª Audit log export
- CSV export (?format=csv): RFC-4180 compliant, proper escaping
- JSONL export (?format=jsonl): one event per line, ISO timestamps
- Content-Disposition with dated filenames
- Export actions generate dmin_action audit events
- 10 new admin e2e tests

## Round 45 ¡ª Regression sweep + cycle report
- Full regression: lint, unit, build, bundle stats, e2e (all green)
- Cycle 09 summary report

## Health snapshot (R1 ¡ú R45)
- Unit tests: **266/266** across 32 files
- Admin e2e: **54/54**
- Security e2e: **31/31**
- CORS e2e: **10/10**
- Browser e2e: **29/29** (visual regression harness)
- Lint: **0 errors**, 23 warnings (transitional)
- Build: **19 routes** (13 dynamic + 6 static), ~795 KB client JS
- All commits on master at https://github.com/Zhi-Chao-PAN/launchlens-research-studio

## Existing problem list
- e2e-02-form-filled.png and e2e-03-research-started.png still
  drift under visual-diff (harness is report-only, non-fatal)
- /api/vitals and /api/telemetry ingest is in-memory only
- provider:openai and provider:anthropic breakers reset on fresh
  Node process unless LAUNCHLENS_STORAGE_DIR is set
- OpenAI streaming SSE reader lacks reconnect (transient drop ¡ú mock fallback)
- No admin UI ¡ª all token/audit management is API-only
- Webhook delivery has no retry queue (fire-and-forget)

## Next thread (R46+)
The next strongest pull is **admin settings UI** ¡ª a browser-facing
page for token management and security observability. After that:
- R47: webhook retry queue with exponential backoff
- R48: SSE reconnect for OpenAI streaming reader
- R49: trusted IP ranges for rate limit bypass
- R50: regression sweep + cycle 10 report